export enum TransactionStatusEnum {
  pending = 'В ожидании',
  completed = 'Выполнен',
  failed = 'Отказ',
}
export enum TransactionStatusServiceEnum {
  PENDING = 'pending',
  COMPLETED = 'completed',
  FAILED = 'failed',
}
